# Security

Do not report secrets or private memory content in issues.

Security-sensitive areas include:
- credential handling
- shell command execution
- worktree ownership
- repository write boundaries
- MCP/tool permissions
- recursive agent invocation
- memory promotion and sanitization

A future release should add a private vulnerability reporting channel before broad public use.
